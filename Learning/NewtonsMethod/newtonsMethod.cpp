 #include <iostream>
#include <string>
#include <iomanip>
#include <math.h> 
using namespace std;

int main() {

    string function, derivative;
    float approximation; 
    cout << "Enter f(x): " << endl;
    cin >> function;

    cout << "Enter f'(x): " << endl;
    cin >> derivative;

    cout << "Enter your first approximation: " << endl; 
    cout << approximation;

    return 0;
}